"use client";

// Email/password auth has been removed. Authentication is now GitHub-only via Supabase.
// This file is kept as a stub to avoid broken imports during the migration.

/** @deprecated Email/password auth removed. Use GitHub OAuth via /login. */
export function AuthEmailPasswordPanel() {
  return null;
}
